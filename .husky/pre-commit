#!/usr/bin/env sh

if ! command -v node >/dev/null 2>&1; then
	echo "[pre-commit] Node.js not found in PATH. Skipping feed sync check."
	exit 0
fi

node scripts/generate-feeds-js.mjs --force || exit 1
node scripts/validate-feed-config.mjs --strict || exit 1
git add public/feeds.js
